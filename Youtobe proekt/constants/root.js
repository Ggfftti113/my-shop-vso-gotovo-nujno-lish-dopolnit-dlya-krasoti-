const ROOT_HEADER = document.getElementById('header');
const ROOT_SPA = document.getElementById('spa-view');
const ROOT_FOOTER = document.getElementById('footer');
// component roots inside SPA are created dynamically (#products-root, #shopping-root, #favorites-root, #profile-root)
